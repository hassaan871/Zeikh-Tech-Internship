const arr = ['a', 'b', 'c', 'd', 'e'];
const arrLength = arr.length;

arr.length = 2;
console.log(`arrLength02: ${arr}`);
console.log(`arrLength02: ${arr.length}`);

console.log(`arrLength: ${arrLength}`);
